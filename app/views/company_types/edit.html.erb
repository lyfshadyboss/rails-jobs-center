<div class="panel panel-info admin-panel-info">
  <div class="panel-heading">
    修改专业
  </div>

  <div class="panel-body"></div>

  <%= form_for([current_admin, @company_type],
               url: admin_company_type_path(@company_type),
               html: {class: 'form-horizontal', role: 'form'}) do |f| %>
      <%= render partial: "company_types/form", locals: {form: f} %>
  <% end %>

</div>